worker_processes 2;
error_log /var/logs/nginx/mydomain.error.log;
pid /var/run/nginx.pid;
include /usr/share/nginx/modules/*.conf; # See /usr/share/doc/nginx/README.dynamic.

events {
  worker_connections 1024; ## Default: 1024
  use epoll; # http://nginx.org/en/docs/events.html
  multi_accept on; # http://nginx.org/en/docs/ngx_core_module.html#multi_accept
}

# Core Modules Docs:
# http://nginx.org/en/docs/http/ngx_http_core_module.html
http {
  include '/etc/nginx/mime.types';
  default_type application/octet-stream;

  keepalive_timeout 65;
  keepalive_requests 100000;
  tcp_nopush on;
  tcp_nodelay on;



  # Nginx `listener` block
  server {
    listen [::]:80 default_server;
    listen 80;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_comp_level 9;
    etag on;


    # Reverse Proxy for `orthanc` APIs (including DICOMWeb)
    #
    location /pacs/ {

      proxy_http_version 1.1;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      expires 0;
      add_header Cache-Control private;

      # Add CORS headers
      # Note: uncomment the following line to allow all domains to access the Orthanc APIs
      # You should actually only allow the domains you trust to access the APIs
      # add_header 'Access-Control-Allow-Origin' '*' always;

      proxy_pass http://orthanc:8042/;

      # By default, this endpoint is protected by CORS (cross-origin-resource-sharing)
      # You can add headers to allow other domains to request this resource.
      # See the "Updating CORS Settings" example below
      add_header 'Access-Control-Allow-Origin' '*' always;
    }

    # Never cache the HTML shell, runtime config, or legacy service worker entry.
    location = /index.html {
      root /var/www/html;
      add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
      expires off;
    }

    location = /app-config.js {
      root /var/www/html;
      add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
      expires off;
    }

    location = /sw.js {
      root /var/www/html;
      add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
      proxy_cache_bypass $http_pragma;
      proxy_cache_revalidate on;
      expires off;
      access_log off;
    }

    # Hashed bundles can be cached aggressively.
    location ~* \.(?:bundle\.[A-Fa-f0-9]+\.(?:js|css)|[A-Fa-f0-9]+\.wasm)$ {
      root /var/www/html;
      add_header Cache-Control "public, max-age=31536000, immutable" always;
      expires 1y;
    }

    # Other static assets may be cached, but should still revalidate.
    location ~* \.(?:js|css|png|jpg|jpeg|gif|ico|svg|webp|json|woff2?|ttf|otf|wasm)$ {
      root /var/www/html;
      add_header Cache-Control "public, max-age=3600, must-revalidate" always;
      expires 1h;
    }

    # Single Page App
    # Try files, fallback to index.html
    #
    location / {
      root /var/www/html;
      index index.html;
      try_files $uri $uri/ /index.html;
      add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0" always;
    }

  }
}
